name: CI Pipeline

# trigger pipeline on push or pull request
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

# define the "Jobs"
jobs:
  test-auth-service:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Install dependencies
        run: cd services/auth-service && npm install
      - name: Run Linter
        run: cd services/auth-service && npx eslint .
      - name: Run unit tests
        run: cd services/auth-service && npm run test:unit
      - name: Run integration tests
        run: cd services/auth-service && npm run test:integration
      - name: Build Docker Image
        run: cd services/auth-service && docker build -t auth-service:test .

  test-user-service:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Install dependencies
        run: cd services/user-service && npm install
      - name: Run Linter
        run: cd services/user-service && npx eslint .
      - name: Run unit tests
        run: cd services/user-service && npm run test:unit
      - name: Run integration tests
        run: cd services/user-service && npm run test:integration
      - name: Build Docker Image
        run: cd services/user-service && docker build -t user-service:test .

  test-task-service:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Install dependencies
        run: cd services/task-service && npm install
      - name: Run Linter
        run: cd services/task-service && npx eslint .
      - name: Run unit tests
        run: cd services/task-service && npm run test:unit
      - name: Run integration tests
        run: cd services/task-service && npm run test:integration
      - name: Build Docker Image
        run: cd services/task-service && docker build -t task-service:test .
